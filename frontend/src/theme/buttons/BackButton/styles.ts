import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  back:{
    width: 120,
    flexDirection: "row",
    position: "absolute",
  },
  backText:{
    fontSize: 24,
    textAlign: "center",
    textAlignVertical: "center",
  }
});